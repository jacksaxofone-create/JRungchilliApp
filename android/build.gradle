buildscript {
    ext {
        buildToolsVersion = "36.0.0"
        minSdkVersion = 24
        compileSdkVersion = 36
        targetSdkVersion = 36
        ndkVersion = "27.1.12297006"
        kotlinVersion = "2.1.20"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
    }
}

apply plugin: "com.facebook.react.rootproject"

// Fix: jcenter() was removed in Gradle 9+. Override repositories for ALL subprojects
// so that legacy libraries (e.g. @brooons/react-native-bluetooth-escpos-printer) that
// still reference jcenter() will resolve against mavenCentral() instead.
allprojects {
    configurations.all {
        resolutionStrategy.eachDependency { details ->
            // Force AndroidX support-v4 migration
            if (details.requested.group == 'com.android.support' && details.requested.name == 'support-v4') {
                details.useTarget('androidx.legacy:legacy-support-v4:1.0.0')
                details.because('Migrating com.android.support to AndroidX')
            }
        }
    }

    // Redirect any jcenter() call from sub-modules to mavenCentral()
    // This is applied AFTER each sub-project evaluates its own buildscript
    buildscript {
        repositories {
            google()
            mavenCentral()
        }
    }
    repositories {
        google()
        mavenCentral()
        maven { url "https://jitpack.io" }
        maven { url "$rootDir/../node_modules/react-native/android" }
    }
}
